name: my first workflow

on: push

jobs:
    first_job:
        runs-on: ubuntu-latest
        steps:
            - name: Checkout Repo
              uses: actions/checkout@v4


            - name: Welcome Message
              run: echo "my first githib actions job this"
            
            - name: List files
              run: ls
            
            - name: Read File
              run: cat README.md 